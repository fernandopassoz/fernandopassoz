{
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "npm install -g parcel && parcel build src/index.html --dist-dir docs --public-url '.'",
    "deploy": "docker-compose -f ./docker-compose.yaml build && docker-compose -f ./docker-compose.yaml push"
  }
}
