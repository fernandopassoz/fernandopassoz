{
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0"
  },
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "npm install -g @parcel/transformer-sass parcel && parcel build src/index.html --dist-dir docs --public-url '.'",
    "deploy": "docker-compose -f ./docker-compose.yaml build && docker-compose -f ./docker-compose.yaml push"
  }
}
